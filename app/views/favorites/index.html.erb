<h1>「<%= @user.name %>」のお気に入り一覧</h1>

<% @favorite_list.each do |favotite| %>
  <p>①投稿者:<%= favotite.user.name %></p>
  <p>②内容:<%= favotite.content %></p>
  <p>③画像:<%= favotite.image %></p>
  <br>
<% end %>

<%= link_to '投稿一覧', posts_path, class:"btn btn-primary" %>
